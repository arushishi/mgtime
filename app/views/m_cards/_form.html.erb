<%= form_for(@m_card) do |f| %>
  <% if @m_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m_card.errors.count, "error") %> prohibited this m_card from being saved:</h2>

      <ul>
      <% @m_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1>Listing m_cards</h1>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @m_cards.each do |m_card| %>
      <tr>
        <td><%= link_to 'Show', m_card %></td>
        <td><%= link_to 'Edit', edit_m_card_path(m_card) %></td>
        <td><%= link_to 'Destroy', m_card, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New M card', new_m_card_path %>

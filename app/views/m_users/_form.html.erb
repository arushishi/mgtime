<%= form_for(@m_user) do |f| %>
  <% if @m_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@m_user.errors.count, "error") %> prohibited this m_user from being saved:</h2>

      <ul>
      <% @m_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
